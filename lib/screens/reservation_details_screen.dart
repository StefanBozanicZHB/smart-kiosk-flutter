import 'package:flutter/material.dart';

class ReservationDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reservation details'),
      ),
      body: Center(child: Text('Detalji'),),
    );
  }
}
